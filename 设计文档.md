# 霖州蒋默 · 数字世界引擎 —— 设计文档

> v0.1 · 2026-09-10
> 本文档是开发的唯一依据。所有实现以本文为准，不以任何已有第三方实现为准。

---

## 一、这是什么

为《霖州蒋默》角色卡开发的**独立弹窗小手机**，属于一个通用架构——**数字世界引擎**——的第一个应用（微信）。

- 核心玩法：玩家在小手机里发消息 → 引擎**单独调一次生成**（不占主对话回合）→ NPC 回复显示在手机里 → 同时写成一条楼层，主 AI 下一回合自然读到。
- 远期：论坛、外卖等是同一引擎的新"应用"，只加数据和输出契约，引擎不改。

## 二、参考

- 运行于酒馆助手（JS-Slash-Runner）卡片脚本环境，绑定角色卡，非全局。
- 使用的酒馆平台 API 见 §四。各 API 行为以本地酒馆版本实测为准。

## 三、名词

| 名词 | 含义 |
|---|---|
| 引擎 | 弹窗手机本体：读取世界书、装配提示词、调生成、写楼层、渲染气泡 |
| 装载器 loader | 贴在角色卡上的小脚本：从 GitHub CDN 拉取引擎本体并执行 |
| 通讯录条目 | 世界书中一条特殊条目，含全部 IF 线的联系人/群数据 |
| 表情包条目 | 世界书中一条特殊条目，表情包名→catbox 文件名 |
| 人设条目 | 世界书中按约定命名的条目，作为独立生成的角色资料来源 |
| 世界线 | 4 条 IF 线之一；由主世界书条目激活状态决定 |

## 四、运行环境

- 注入方式：酒馆助手（JS-Slash-Runner）卡片脚本，绑定角色卡，非全局。
- 脚本运行在 iframe 沙盒中，通过代理层使用酒馆 API：
  - 世界书：`getCharWorldbookNames('current')` / `getWorldbook(name)`（含容错适配多个函数名）
  - 消息：`getChatMessages('0-{{lastMessageId}}')`、`createChatMessages([...])`
  - 生成：`generateRaw({ ordered_prompts, should_silence, max_chat_history: 0 })`
  - 事件：`tavern_events.WORLD_INFO_ACTIVATED` / `CHAT_CHANGED` / `MESSAGE_RECEIVED` 等
  - 存储：`getVariables({type:'chat'})` / `replaceVariables(...)`（聊天级，随卡走）
  - 主页面 DOM：`window.parent.$` / `window.parent.document`（渲染气泡用）
- 本体托管：用户自己的 GitHub 仓库，经 jsDelivr 三域加载。

## 五、数据协议

### 5.1 世界书条目约定（全部用条目备注/标题识别）

| 条目备注 | 作用 |
|---|---|
| `霖州蒋默::通讯录` | 全部世界线的联系人/群，JSON，键=主条目名 |
| `霖州蒋默::表情包` | 表情包名→文件名，JSON 或 `名字: 文件名` 逐行 |
| `霖州蒋默::人设::蒋默` | 角色「蒋默」的生成资料原文（可多条，按名匹配） |

### 5.2 通讯录格式（字段就三个，不加戏）

```json
{
  "DLC·高中": {
    "contacts": [ {"name": "蒋默", "avatar": "xxxx.jpg"} ],
    "groups":   [ {"name": "高三（2）班", "members": ["蒋默"]} ]
  },
  "DLC·大学": { "contacts": [], "groups": [] },
  "DLC·成人": { "contacts": [], "groups": [] }
}
```

- `avatar` 为 catbox 文件名，引擎拼 `https://files.catbox.moe/` 前缀。
- 图片资源图床可配置（`IMG_BASE`），换图床只改配置。

### 5.3 世界线判定

- 酒馆每次 AI 生成时广播 `WORLD_INFO_ACTIVATED`，附激活条目列表（含备注）。
- 引擎监听该事件，备注匹配三个主条目名（【DLC·高中】【DLC·大学】【DLC·成人】）→ 定为当前世界线，取通讯录对应区块。
- `DLC·高中` 是挂名条目（内容随意，引擎只认标题）：正常游玩恰好开一条；全部关闭或同时多开视为「读不出」，按聊天记录记录归位。
- 首次进卡尚无生成事件时：沿用聊天变量里上次记录；都没有则提示未定位，首次生成后自动归位。

### 5.4 时间锚点与情境

- 时间：解析**最新楼层** `<status>` 中 `<环境>` 行的游戏内时间（`2034年8月26日 星期五|22:49|…`）。取不到则不带时间。
- NPC 情境：取状态栏中**属于该角色的小块**（`<蒋默>…位置…姿态…`）——**只取位置和姿态，绝不取心声**（防开天眼）。
- 注意：`<环境>` 的地点是 **user 所在处**，不是 NPC 所在处，不得混用。

### 5.5 楼层记录格式

```
[📱与蒋默的私聊 22:49]
persona名：在吗
蒋默：刚写完卷子，怎么啦
[/📱]
```

- 群聊头部为 `[📱群名 群聊 22:49]`。
- 消息类型行（双方通用语法）：
  - `[表情:名字]` `[语音:内容]` `[图片:描述]` `[戳一戳]` `[定位:地点]`
- 每条记录为**独立楼层**（system 角色、非隐藏），整层楼只含此块，引擎把整块替换成气泡样式。
- "匹配不到表情包的，保留 `[表情:名字]` 原文，不丢弃"。

### 5.6 聊天记录存储

- 存聊天变量 `lzjm_phone`：`{ "蒋默": [消息…], "group:高三（2）班": [消息…] }`。
- 消息：`{ who, kind, text, time }`，每会话留最近 100 条。

## 六、生成装配（提示词 v0 · 待实测迭代）

框架：**数字世界模拟**（非跑团扮演），引擎不认角色、只认「应用 + 实体 + 资料」。

```
【数字世界 · 回应生成】
你是数字生活世界的模拟引擎。本次任务：生成应用「微信」中
来自「蒋默」的新消息。

【实体资料】…（来自 霖州蒋默::人设::蒋默）
【情境】当前时间 … ·（该实体的位置/姿态，如有）
【应用内记录】…（手机聊天记录最近 N 条）
【主线近貌】…（最近 8 楼正文，去标签每楼≤900字；仅背景）
【一致性铁律】只知道自己在场的事；其他会话不可见；拿不准就装不知道
【输出契约】1~4 条、每行一条、≤35 字、类型语法、禁止开场寒暄
```

- 群聊：注入每个在场成员的「人设条目」摘要 + 群定义，输出 `名字：内容` 多行。
- 温度等参数为引擎常量，后续做设置面板。

## 七、装载器（自研）

- 防重复注入：`window.__LZJM__` 守卫。
- 版本：GitHub API 取 main 最新提交号（5s 超时）→ 失败退回 `@main`；无 Supabase。
- 三域 jsDelivr 依次拉取（各 10s 闸刀）；成功则缓存 `localStorage.lzjm_dist_cache`。
- 全失败 → 用缓存副本离线运行（toastr 提示）；连缓存都没有 → toastr + 悬浮重试按钮。
- `//# sourceURL=` 便于调试。

## 八、UI 概要（全部自有设计）

- 悬浮球：可拖拽圆形按钮；点击开手机。
- 手机壳：现代简洁风格深色壳；状态栏显示**游戏内时间**。
- 屏：首页（应用图标）→ 微信会话列表 → 聊天窗（气泡/输入框/表情面板）。
- 聊天气泡同时在主聊天界面渲染（独立楼层整块替换）。

## 九、已确认的坑（来自她的验尸报告，作为风险清单而非代码参考）

1. 上下文每楼只截 320 字 ≈ 失明 → 我们每楼 900 字。
2. 模型爱编表情包名 → 白名单 + 同义词 + 模糊匹配 + 兜底保留文字。
3. 楼层末尾常有正则隐藏尾巴，插入内容必须整体独立成楼。
4. NPC 开天眼 → 一致性铁律 + 状态栏心声不外传。

## 十、待定/待实测

- [ ] 提示词 v0 实际效果调优（用户已表态需重写打磨）
- [ ] NPC 情境块的准确定位（状态栏格式变动时）
- [ ] persona 名与头像在沙盒中的可靠取法（v0 降级为"我"+字母头像）
- [ ] 表情包条目的真实格式（用户提供后定稿解析）
- [ ] 通用 NPC 头像/背景池（开放群随机路人）——二期
- [ ] 论坛应用——二期
